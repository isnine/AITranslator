# AITranslator Environment Configuration
# ======================================
#
# Copy this file to `.env` and fill in your values.
# This file is git-ignored and should never be committed.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   make secrets
#
# The secrets will be injected into Configuration/Secrets.xcconfig
# which is then used by Xcode during the build process.

# =============================================================================
# REQUIRED: Cloud Service Authentication
# =============================================================================

# HMAC signing secret for the built-in cloud translation service
# This is required for the app to authenticate with the cloud API
AITRANSLATOR_CLOUD_SECRET=

# =============================================================================
# OPTIONAL: Additional Configuration
# =============================================================================

# Override the cloud service endpoint URL
# Default: https://translator-api.zanderwang.com
# AITRANSLATOR_CLOUD_ENDPOINT=https://your-custom-endpoint.com
