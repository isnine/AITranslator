<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
        AITranslator Secrets Configuration
        ==================================
        
        Copy this file to "Secrets.plist" and fill in your API keys.
        
        IMPORTANT: Never commit Secrets.plist to version control!
        The .gitignore file is configured to exclude it.
        
        Alternative: You can also set environment variables instead of using this file:
        - AITRANSLATOR_CLOUD_SECRET    (HMAC secret for cloud service)
        - AITRANSLATOR_CLOUD_TOKEN     (Authentication token)
        - AITRANSLATOR_CLOUD_ENDPOINT  (Optional: override cloud endpoint URL)
        
        For local development, you can place Secrets.plist in either:
        1. The app bundle (add to Xcode project, but NOT to version control)
        2. The App Group container (for sharing with extensions)
    -->
    
    <!-- Required: HMAC secret for built-in cloud service authentication -->
    <key>BuiltInCloudSecret</key>
    <string>YOUR_HMAC_SECRET_HERE</string>
    
    <!-- Optional: Authentication token for cloud service -->
    <key>CloudToken</key>
    <string></string>
    
    <!-- Optional: For direct Azure OpenAI access (not needed for built-in cloud) -->
    <key>AzureApiKey</key>
    <string></string>
    
    <key>AzureEndpoint</key>
    <string></string>
</dict>
</plist>
